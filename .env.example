# 油换电计算器 — 前端环境变量配置示例
# 复制此文件为 .env.local 并填写实际值

# Cloudflare Worker 的服务地址
# 本地开发时（配合 wrangler dev）：
VITE_WORKER_URL=http://localhost:8787

# 部署到 Cloudflare Pages 后，替换为 Worker 实际域名，例如：
# VITE_WORKER_URL=https://ev-calc-worker.your-subdomain.workers.dev
